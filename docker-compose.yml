services:
  app:
    build: .
    image: zerosite:latest
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - zerosite-config:/app/public/js
      - zerosite-data:/app/data

volumes:
  zerosite-config:
  zerosite-data:
